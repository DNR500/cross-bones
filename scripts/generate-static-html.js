const bundlesUtils = require('./utils/bundle-utils');

// bundlesUtils.getHashedFilenames('main.css', 'main.js').then((filename) => {
//     console.info(filename);
// }).catch((err) => {
//     console.error(err);
// });

// create list of scripts to include

// create list of styles

const about = require('../src/public/about.html.js');

about();